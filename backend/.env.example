# Django Settings
DEBUG=False
SECRET_KEY=your-super-secret-key-here
ALLOWED_HOSTS=your-railway-domain.up.railway.app,localhost,127.0.0.1

# Database Settings (Railway PostgreSQL)
DB_NAME=postgresql
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=containers-us-west-1.railway.app
DB_PORT=5432
DB_SSL=true

# Optional: Weather API Key (for weather features)
OPENWEATHER_API_KEY=your-openweather-api-key

# Optional: Google Translate API (if needed)
GOOGLE_TRANSLATE_API_KEY=your-google-api-key

# ML Model Download Configuration
# Choose ONE method for each model (Google Drive OR Direct URL)

# TensorFlow Model (CropLeaf-C1.h5)
# Option 1: Google Drive (recommended - more reliable)
TF_MODEL_GOOGLE_DRIVE_ID=your-tensorflow-model-google-drive-file-id
# Option 2: Direct download URL
# TF_MODEL_URL=https://example.com/path/to/CropLeaf-C1.h5

# PyTorch Model (plant_disease_model_1_latest.pt)
# Option 1: Google Drive (recommended - more reliable)
PT_MODEL_GOOGLE_DRIVE_ID=your-pytorch-model-google-drive-file-id
# Option 2: Direct download URL
# PT_MODEL_URL=https://example.com/path/to/plant_disease_model_1_latest.pt

# Instructions:
# 1. Upload your model files to Google Drive
# 2. Make them publicly accessible (share link)
# 3. Extract the file ID from the share link: https://drive.google.com/file/d/FILE_ID/view
# 4. Set the corresponding environment variable above
# 5. Run: python download_models.py during deployment